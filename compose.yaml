services:
  api:
    build: .
    container_name: maintenance-api
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 3000
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
